import * as React from 'react';
import { FormContainer } from '../components/FormContainer';

export const Home = () => {
  return (
    <FormContainer>
      <div>HOME</div>
    </FormContainer>
  );
};
